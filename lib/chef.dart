import 'package:flutter/material.dart';

class ChefPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
        children: [Icon(Icons.directions_car, size: 200), Text('Chefs Menu')]);
  }
}
